# Changelog

## v1.0.2 - 2026-08-06

- 新增“允许分段模型修改正文”选项，默认关闭并保持严格的逐字符校验。
- 宽松模式允许采用模型对文字、空格和标点的改动；正文长度变化超过原文 10% 时回退，不再设置固定字符容差。
- 修复宽松模式可能从空正文生成内容，以及把分段模型新增的字面量换行直接作为可见正文发送的问题。
- 使用 `<bubble-cr/>`、`<bubble-lf/>`、`<bubble-crlf/>`、`<bubble-lfcr/>` 分别保护主 LLM 原文中的 `\r`、`\n`、`\r\n`、`\n\r`，关闭转义换行识别时可按原样恢复。
- 分段模型新增的字面量换行统一视为分段意图并转换为真实换行；候选被拒绝或模型调用失败时，原文字面量仍按配置处理。
- 颜文字标签禁止在内部插入真实换行。严格模式同时保护标记位置；宽松模式允许标记随正文调整位置，但仍校验标记种类、数量和标签结构。
- 非布尔流式配置值改为按未知状态保守处理，避免在无法确认发送方式时注入内部颜文字协议。
- 智能引用观察器不再主动排除平台回送的 bot 自身入站事件；直接发送的回复和主动消息不会自动进入该观察器。
- 主 LLM 颜文字协议补充“换行只能位于标签整体之前或之后”的约束。
- 保留协议标签和 Markdown 包装保护，并补充测试职责说明。
- 日志标记分段候选使用严格或宽松校验，且不记录候选正文。

## v1.0.1 - 2026-07-29

- 新增按纯文本字数计算气泡间隔，并支持最短、最长间隔限制。
- 新增随机浮动，可同时作用于固定间隔和按字数间隔。
- 默认继续使用固定 `0.8` 秒，兼容只配置 `delay_seconds` 的现有配置。

## v1.0.0 - 2026-07-18

- 支持按真实换行、自定义标记及独立模型建议规划自然聊天分段。
- 对模型给出的分段结果执行严格校验，只接受未改写正文的换行调整。
- 支持图片、平台表情、At 和 Reply 的位置规划；特殊组件可安全交回 AstrBot 发送。
- 发送前预先保留剩余消息，降低异常或任务取消造成重复发送的风险。
- 支持始终引用和智能引用；智能引用会观察同一会话中的新入站消息。
- 支持固定气泡间隔，并在延迟被取消时保留尚未发送的结果。
- 支持会话黑名单，使用 `G:群号` 和 `F:QQ号` 区分群聊与私聊。
- 支持清理每段首尾空格及指定段尾字符；清理后为空的分段不会发送。
- 混合组件的空格清理仅作用于整段边界，不改变组件内部正文。
- 流式回复自动跳过分段和颜文字协议，避免内部标签进入可见内容。
- 默认日志不记录完整正文和会话标识，并支持多 Bot 日志前缀与调试日志提级。
- 配置项提供用途、风险和平台边界提示。
