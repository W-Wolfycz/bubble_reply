# Changelog

## v1.0.0 - 2026-07-18

- 插件正式命名为 BubbleReply（灵动分段），使用独立插件标识、日志前缀和内部协议标签。
- 采用独立分层架构，解耦文本规则、分段规划、发送编排与 AstrBot 集成。
- 新增严格的 layout-only LLM 候选校验器。
- 新增组件规划器和特殊组件 RespondStage 降级。
- 新增在发送副作用前提交 fallback 的 at-most-once 状态机。
- 新增容量受限的 smart reply LRU tracker。
- 默认日志改为不包含正文与完整会话标识。
- 补充详细配置 hint，并显眼标注破坏性清理、smart reply 边界和正文日志风险。
- smart reply 改为观察同一 UMO 的全部入站消息，而非只观察 LLM 请求。
- 主动发送延迟简化为单一固定间隔，移除 random/linear/log 配置。
- 日志配置支持机器人实例前缀与调试日志提级。
- 配置提示改为面向使用者的简短说明，组件策略和引用方式使用中文选项。
- 会话黑名单改为 `G:群号` 与 `F:QQ号` 格式。
- 新增“清理首尾空格”开关，默认开启；清理后为空的分段不会发送。
- 修正混合组件中的空格清理边界，只处理整段首尾并在空组件后继续向内清理。
- 根据会话、事件和平台流式设置自动跳过颜文字协议，避免内部标签进入流式回复。
